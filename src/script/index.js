function openModal() {

}

function initAddEventsList() {
    document.querySelectorAll(".card").forEach((card) => {
        card.addEventListener("click", (e) => {
            if (e.target.closest(".edit")) {
                
                return;
            }
        });
    });
}

function validate() {
    const form= document.find
}

initAddEventsList()